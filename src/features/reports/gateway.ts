import type { SupabaseClient } from '@supabase/supabase-js';
import { z } from 'zod';
export type ReportInput = { targetType: 'listing' | 'conversation' | 'barter' | 'order' | 'review'; targetId: string; reason: 'scam' | 'unsafe' | 'not_as_described' | 'harassment' | 'other'; description: string };
export type ReportResult = { id: string; status: 'open' };
export type ReportEvidenceInput = { sourceType: 'listing' | 'message' | 'barter_event' | 'order_event'; sourceId: string; note: string };
export interface ReportGateway { create(input: ReportInput): Promise<ReportResult>; attachEvidence(reportId: string, input: ReportEvidenceInput): Promise<{ id: string }>; }
export function createSupabaseReportGateway(client: SupabaseClient): ReportGateway { return { async create(input) { const { data, error } = await client.rpc('create_report', { p_target_type: input.targetType, p_target_id: input.targetId, p_reason: input.reason, p_description: input.description }); const parsed = z.object({ id: z.string().uuid(), status: z.literal('open') }).safeParse(data); if (error || !parsed.success) throw new Error('Laporan belum dapat dikirim.'); return parsed.data; }, async attachEvidence(reportId, input) { const { data, error } = await client.rpc('attach_report_evidence', { p_report_id: reportId, p_source_type: input.sourceType, p_source_id: input.sourceId, p_note: input.note }); const parsed = z.object({ id: z.string().uuid() }).safeParse(data); if (error || !parsed.success) throw new Error('Bukti belum dapat dilampirkan.'); return parsed.data; } }; }
