import type { SupabaseClient } from '@supabase/supabase-js';
import { z } from 'zod';
import type { OrderItemInput, OrderQuoteInput, OrderRoom } from './types';

const itemSchema = z.object({ id: z.string().uuid(), listingId: z.string().uuid(), variantId: z.string().uuid().nullable(), name: z.string(), unit: z.string(), quantity: z.number().int().positive(), unitPriceRupiah: z.string(), lineTotalRupiah: z.string() });
const roomSchema: z.ZodType<OrderRoom> = z.object({
  id: z.string().uuid(), conversationId: z.string().uuid(), listingId: z.string().uuid(), kind: z.enum(['sale', 'free']), lifecycle: z.enum(['quoted', 'confirmed', 'awaiting_dp', 'processing', 'ready', 'awaiting_receipt', 'completed', 'cancelled']), revision: z.number().int().positive(), acceptedRevision: z.number().int().positive().nullable(), actorRole: z.enum(['buyer', 'seller']), buyer: z.object({ id: z.string().uuid(), name: z.string() }), seller: z.object({ id: z.string().uuid(), name: z.string() }), items: z.array(itemSchema), terms: z.object({ handoverMethod: z.enum(['pickup', 'meetup', 'delivery']), handoverNote: z.string(), shippingAmountRupiah: z.string(), subtotalRupiah: z.string(), totalRupiah: z.string(), dpPercent: z.number().int().min(0).max(100), dpAmountRupiah: z.string(), dpDeadline: z.string().datetime().nullable() }), payments: z.array(z.object({ kind: z.enum(['dp', 'balance', 'shipping']), amountRupiah: z.string(), state: z.enum(['due', 'acknowledged', 'cancelled']), dueAt: z.string().datetime().nullable() })), fulfillment: z.object({ processingAt: z.string().datetime().nullable(), readyAt: z.string().datetime().nullable(), handedAt: z.string().datetime().nullable(), receivedAt: z.string().datetime().nullable() }), receiptFollowUp: z.object({ triggerAt: z.string().datetime(), helpAvailableAt: z.string().datetime(), canRequestAdminHelp: z.boolean(), adminHelpRequested: z.boolean() }).nullable().optional(), cancellationReason: z.string().nullable(), cancellationRequest: z.object({ id: z.string().uuid(), requestedBy: z.enum(['buyer', 'seller']), reason: z.string(), status: z.enum(['pending', 'approved', 'rejected']), createdAt: z.string().datetime() }).nullable(), refundFollowUpRequired: z.boolean(), amendment: z.object({ id: z.string().uuid(), status: z.enum(['proposed', 'accepted', 'rejected', 'withdrawn']), baseRevision: z.number().int().positive(), proposedRevision: z.number().int().positive(), proposerRole: z.enum(['buyer', 'seller']), reason: z.string(), createdAt: z.string().datetime(), expiresAt: z.string().datetime().nullable(), items: z.array(itemSchema).optional(), terms: z.object({ handoverMethod: z.enum(['pickup', 'meetup', 'delivery']), handoverNote: z.string(), shippingAmountRupiah: z.string(), subtotalRupiah: z.string(), totalRupiah: z.string(), dpPercent: z.number().int().min(0).max(100), dpAmountRupiah: z.string(), dpDeadline: z.string().datetime().nullable() }).optional() }).nullable().optional(), refunds: z.array(z.object({ id: z.string().uuid(), status: z.enum(['proposed', 'accepted', 'sent_unconfirmed', 'confirmed', 'rejected']), basis: z.enum(['amendment', 'cancellation', 'admin_decision']), amountRupiah: z.string(), payerRole: z.enum(['buyer', 'seller']), recipientRole: z.enum(['buyer', 'seller']), reason: z.string(), proofNote: z.string().nullable(), createdAt: z.string().datetime(), acceptedAt: z.string().datetime().nullable(), sentAt: z.string().datetime().nullable(), confirmedAt: z.string().datetime().nullable() })).optional(), settlement: z.object({ acknowledgedRupiah: z.string(), confirmedRefundRupiah: z.string(), netReceivedRupiah: z.string(), amountStillDueRupiah: z.string(), refundCapRupiah: z.string() }).optional(), updatedAt: z.string().datetime(),
});
export interface OrderGateway { get(id: string): Promise<OrderRoom>; createQuote(conversationId: string, items: OrderItemInput[], terms: OrderQuoteInput, key: string): Promise<OrderRoom>; confirm(id: string, revision: number, key: string): Promise<OrderRoom>; acknowledgePayment(id: string, revision: number, kind: 'dp' | 'balance' | 'shipping', key: string): Promise<OrderRoom>; markProcessing(id: string, revision: number, key: string): Promise<OrderRoom>; markReady(id: string, revision: number, key: string): Promise<OrderRoom>; markHandedOver(id: string, revision: number, key: string): Promise<OrderRoom>; confirmReceived(id: string, revision: number, key: string): Promise<OrderRoom>; cancel(id: string, revision: number, reason: string, key: string): Promise<OrderRoom>; requestCancellation(id: string, revision: number, reason: string, key: string): Promise<OrderRoom>; respondCancellation(requestId: string, revision: number, approve: boolean, key: string): Promise<OrderRoom>; proposeAmendment(id: string, revision: number, items: OrderItemInput[], terms: OrderQuoteInput, key: string): Promise<OrderRoom>; acceptAmendment(amendmentId: string, revision: number, key: string): Promise<OrderRoom>; rejectAmendment(amendmentId: string, revision: number, key: string): Promise<OrderRoom>; withdrawAmendment(amendmentId: string, revision: number, key: string): Promise<OrderRoom>; proposeRefund(id: string, revision: number, amountRupiah: string, reason: string, basis: 'amendment' | 'cancellation' | 'admin_decision', key: string): Promise<OrderRoom>; acceptRefund(refundId: string, revision: number, key: string): Promise<OrderRoom>; rejectRefund(refundId: string, revision: number, key: string): Promise<OrderRoom>; recordRefundSent(refundId: string, revision: number, proofNote: string, key: string): Promise<OrderRoom>; confirmRefundReceived(refundId: string, revision: number, key: string): Promise<OrderRoom>; requestAdminHelp?(orderId: string, description: string): Promise<void>; subscribe(id: string, onCommittedChange: () => void): () => void; }
const failure = () => new Error('Pesanan belum dapat memproses permintaan.');
export function createSupabaseOrderGateway(client: SupabaseClient): OrderGateway {
  async function command(name: string, payload: Record<string, unknown>) { const { data, error } = await client.rpc(name, payload); const parsed = roomSchema.safeParse(data); if (error || !parsed.success) throw failure(); return parsed.data; }
  return {
    get: id => command('get_order', { p_order_id: id }),
    createQuote: (conversationId, items, terms, key) => command('create_order_quote', { p_conversation_id: conversationId, p_items: items, p_handover_method: terms.handoverMethod, p_handover_note: terms.handoverNote, p_shipping_amount: terms.shippingAmountRupiah, p_dp_percent: terms.dpPercent, p_dp_deadline: terms.dpDeadline, p_reason: terms.reason, p_idempotency_key: key }),
    confirm: (id, revision, key) => command('confirm_order', { p_order_id: id, p_expected_revision: revision, p_idempotency_key: key }),
    acknowledgePayment: (id, revision, kind, key) => command('acknowledge_order_payment', { p_order_id: id, p_expected_revision: revision, p_kind: kind, p_idempotency_key: key }),
    markProcessing: (id, revision, key) => command('mark_order_processing', { p_order_id: id, p_expected_revision: revision, p_idempotency_key: key }),
    markReady: (id, revision, key) => command('mark_order_ready', { p_order_id: id, p_expected_revision: revision, p_idempotency_key: key }),
    markHandedOver: (id, revision, key) => command('mark_order_handed_over', { p_order_id: id, p_expected_revision: revision, p_idempotency_key: key }),
    confirmReceived: (id, revision, key) => command('confirm_order_received', { p_order_id: id, p_expected_revision: revision, p_idempotency_key: key }),
    async requestAdminHelp(orderId, description) {
      const { data, error } = await client.rpc('request_admin_help', { p_target_type: 'order', p_target_id: orderId, p_description: description });
      const parsed = z.object({ id: z.string().uuid(), status: z.literal('open') }).safeParse(data);
      if (error || !parsed.success) throw failure();
    },
    cancel: (id, revision, reason, key) => command('cancel_order', { p_order_id: id, p_expected_revision: revision, p_reason: reason, p_idempotency_key: key }),
    requestCancellation: (id, revision, reason, key) => command('request_order_cancellation', { p_order_id: id, p_expected_revision: revision, p_reason: reason, p_idempotency_key: key }),
    respondCancellation: (requestId, revision, approve, key) => command('respond_order_cancellation', { p_request_id: requestId, p_expected_revision: revision, p_approve: approve, p_idempotency_key: key }),
    proposeAmendment: (id, revision, items, terms, key) => command('propose_order_amendment', { p_order_id: id, p_expected_revision: revision, p_items: items, p_handover_method: terms.handoverMethod, p_handover_note: terms.handoverNote, p_shipping_amount: terms.shippingAmountRupiah, p_dp_percent: terms.dpPercent, p_dp_deadline: terms.dpDeadline, p_reason: terms.reason, p_idempotency_key: key }),
    acceptAmendment: (amendmentId, revision, key) => command('accept_order_amendment', { p_amendment_id: amendmentId, p_expected_revision: revision, p_idempotency_key: key }),
    rejectAmendment: (amendmentId, revision, key) => command('reject_order_amendment', { p_amendment_id: amendmentId, p_expected_revision: revision, p_idempotency_key: key }),
    withdrawAmendment: (amendmentId, revision, key) => command('withdraw_order_amendment', { p_amendment_id: amendmentId, p_expected_revision: revision, p_idempotency_key: key }),
    proposeRefund: (id, revision, amountRupiah, reason, basis, key) => command('propose_refund', { p_order_id: id, p_expected_revision: revision, p_amount_rupiah: amountRupiah, p_reason: reason, p_basis: basis, p_idempotency_key: key }),
    acceptRefund: (refundId, revision, key) => command('accept_refund', { p_refund_request_id: refundId, p_expected_revision: revision, p_idempotency_key: key }),
    rejectRefund: (refundId, revision, key) => command('reject_refund', { p_refund_request_id: refundId, p_expected_revision: revision, p_idempotency_key: key }),
    recordRefundSent: (refundId, revision, proofNote, key) => command('record_refund_sent', { p_refund_request_id: refundId, p_expected_revision: revision, p_proof_note: proofNote, p_idempotency_key: key }),
    confirmRefundReceived: (refundId, revision, key) => command('confirm_refund_received', { p_refund_request_id: refundId, p_expected_revision: revision, p_idempotency_key: key }),
    subscribe(id, onCommittedChange) { const channel = client.channel(`order:${id}`).on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'order_events', filter: `order_id=eq.${id}` }, onCommittedChange).subscribe(); return () => { void client.removeChannel(channel); }; },
  };
}
